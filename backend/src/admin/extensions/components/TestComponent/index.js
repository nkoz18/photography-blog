import React from 'react';

const TestComponent = () => {
  return (
    <div style={{ padding: '16px', background: 'lightgreen', borderRadius: '4px' }}>
      Hello World! If you can see this, the zone is safe.
    </div>
  );
};

export default TestComponent;